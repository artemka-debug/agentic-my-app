#!/usr/bin/env bash
# Phase 3 stub — replace with `git worktree remove` / prune.
set -euo pipefail
RUN_ID="${1:?run id}"
WORKTREES_ROOT="${2:?worktrees root}"
echo "[agentic-task stub] worktree-cleanup: run=${RUN_ID} root=${WORKTREES_ROOT}"
echo "[agentic-task stub] no removal performed"

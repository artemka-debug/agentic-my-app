implementation_plan:
  tasks:
    - id: TASK-001
      objective: Wire GitHub CLI intake paths and deterministic state transitions.
      requirements: [REQ-001]
      suggested_areas:
        - agentic/github_intake.py
      tests:
        - unit: parsers for owner/repo#N and URLs
      dependencies: []

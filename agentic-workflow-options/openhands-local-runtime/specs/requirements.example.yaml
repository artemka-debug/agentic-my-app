requirements_spec:
  requirement_ids:
    - id: REQ-001
      text: User can start a workflow from `owner/repo#123`.
  acceptance_criteria:
    - CLI accepts shorthand and GitHub URLs.
    - Issue title/body/comments surface in TaskBrief `_intake_meta` when using `gh`.
  edge_cases: []
  constraints: []
  definition_of_done: []

# Verification matrix (stub)

Fill this during decomposition. Phase 5 will execute configured commands and map results back to requirements.

| Step | Required | Command / notes | Requirement IDs |
| ---- | -------- | ----------------- | ----------------- |
| Lint | yes | from repo config | |
| Typecheck | yes | from repo config | |
| Unit tests | yes | from repo config | |
| Integration tests | yes | from repo config | |
| Browser / E2E | if UI | from repo config | |
| Code review | yes | harness / agent | |
| Security review | yes | harness / agent | |
| PO acceptance | yes | harness / agent | |

## Edge-case tests

Link to EDGE-IDs from requirements.

## Evidence expectations

List files or tests that should demonstrate coverage for each `REQ-*`.

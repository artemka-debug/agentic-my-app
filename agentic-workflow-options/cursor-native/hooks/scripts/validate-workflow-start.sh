#!/usr/bin/env bash
# Stub: copy to .cursor/hooks/scripts/ and wire from .cursor/hooks.json (see hooks.example.json).
# Intended checks: gh auth, repo root, optional clean working tree — expand per org policy.
set -euo pipefail
echo "[cursor-native stub] validate-workflow-start: add your preflight checks (gh auth, git status)." >&2
exit 0
